"""Experiment engines that back a paper result.

``checkpoint_truncation_replay`` is the engine behind Table 3; ``common`` and
``evaluation`` are its helpers.  Import it as a module:

>>> from hssp_dfl.experiments import checkpoint_truncation_replay
"""
